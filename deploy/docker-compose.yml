services:
  salary-predictor:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - ..:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
